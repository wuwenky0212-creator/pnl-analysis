# 🚂 一键部署到Railway - 最详细指南

## 📋 准备工作

1. **GitHub账号**（如果没有，去 https://github.com 注册）
2. **Railway账号**（免费注册，使用GitHub登录）

---

## 🚀 完整部署流程

### 第一步：上传代码到GitHub

#### 1.1 创建GitHub仓库

1. 访问：https://github.com/new
2. 仓库名：`损益归因分析`（或任何你喜欢的名字）
3. 选择：Public（公开，免费）或 Private（私有）
4. 点击 "Create repository"

#### 1.2 上传代码

在项目目录执行：

```bash
# 初始化Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "初始提交 - 准备部署到Railway"

# 添加远程仓库（替换为你的GitHub用户名和仓库名）
git remote add origin https://github.com/你的用户名/损益归因分析.git

# 推送到GitHub
git branch -M main
git push -u origin main
```

✅ 代码已上传到GitHub！

---

### 第二步：部署后端到Railway

#### 2.1 注册Railway

1. 访问：https://railway.app
2. 点击 "Start a New Project"
3. 选择 "Login with GitHub"
4. 授权Railway访问你的GitHub账号

#### 2.2 创建新项目

1. 在Railway控制台，点击 "New Project"
2. 选择 "Deploy from GitHub repo"
3. 在列表中找到你的仓库："损益归因分析"
4. 点击仓库名称

#### 2.3 配置后端服务

Railway会自动检测到Dockerfile，开始构建。

**等待构建完成**（大约2-5分钟）

#### 2.4 配置环境变量

1. 在服务页面，点击服务名称
2. 选择 "Variables" 标签
3. 点击 "New Variable"，添加以下变量：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `PORT` | `8002` | 端口号（Railway也会自动设置） |
| `HOST` | `0.0.0.0` | 监听地址 |
| `DEBUG` | `False` | 生产环境关闭调试 |
| `DATA_FORMAT` | `mock` | 使用模拟数据 |
| `DEFAULT_CURRENCY` | `CNY` | 默认货币 |
| `DEFAULT_UNIT` | `万元` | 默认单位 |

4. 点击 "Save"

#### 2.5 获取后端URL

1. 在服务设置中，点击 "Settings"
2. 向下滚动找到 "Domains"
3. 点击 "Generate Domain"
4. Railway会生成一个域名，例如：
   - `https://fx-attribution-backend-production.up.railway.app`
5. **复制这个URL**，后面会用到！

✅ 后端部署完成！

---

### 第三步：部署前端到Railway

#### 方法A：使用Railway静态站点（推荐）

1. 在Railway项目中，点击 "New"
2. 选择 "Empty Service"
3. 配置：
   - **Source**: 选择你的GitHub仓库
   - **Root Directory**: `frontend`
   - **Start Command**: （留空）
4. 点击 "Deploy"

#### 方法B：使用Nginx容器（更灵活）

1. 在Railway项目中，点击 "New"
2. 选择 "Empty Service"
3. 配置：
   - **Source**: 选择你的GitHub仓库
   - **Root Directory**: `frontend`
   - **Dockerfile**: （创建新的Dockerfile，见下方）

创建 `frontend/Dockerfile`：

```dockerfile
FROM nginx:alpine
COPY . /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

#### 方法C：使用Vercel（推荐，前端更快）

1. 访问：https://vercel.com
2. 使用GitHub登录
3. 点击 "Add New Project"
4. 导入你的GitHub仓库
5. 配置：
   - Framework Preset: **Other**
   - Root Directory: `frontend`
   - Build Command: （留空）
   - Output Directory: `.`
6. 在 "Environment Variables" 中添加：
   ```
   API_URL=https://你的后端URL.railway.app/api
   ```
7. 点击 "Deploy"

---

### 第四步：配置前端API地址

#### 如果使用Railway部署前端

修改 `frontend/index.html`，在 `<head>` 中添加：

```html
<meta name="backend-url" content="https://你的后端URL.railway.app">
```

例如：
```html
<meta name="backend-url" content="https://fx-attribution-backend-production.up.railway.app">
```

#### 如果使用Vercel部署前端

不需要修改代码，Vercel会自动使用环境变量。

---

### 第五步：提交更新

```bash
# 修改了代码后
git add .
git commit -m "配置后端API地址"
git push
```

Railway和Vercel会自动检测并重新部署！

---

## 🎉 部署完成！

### 访问地址

- **前端页面**: 
  - Railway: https://你的前端URL.railway.app
  - Vercel: https://your-app.vercel.app
- **后端API**: https://你的后端URL.railway.app
- **API文档**: https://你的后端URL.railway.app/docs

### 测试访问

1. 打开前端URL
2. 打开浏览器开发者工具（F12）
3. 查看Network标签，确认API请求成功
4. 如果看到数据，说明部署成功！

---

## 📊 监控和管理

### 查看日志

1. 在Railway控制台选择服务
2. 点击 "Deployments"
3. 选择最近的部署
4. 查看实时日志

### 查看指标

- CPU使用率
- 内存使用率
- 网络流量
- 请求数量

### 重启服务

在服务设置中点击 "Restart"

---

## 🔒 安全建议

1. **环境变量**：不要将敏感信息提交到Git
2. **CORS设置**：生产环境限制允许的域名
3. **API限流**：考虑添加请求频率限制
4. **监控**：定期查看日志和指标

---

## 💰 费用说明

### Railway免费额度

- **$5/月免费额度**
- 通常足够个人项目使用
- 超出后按使用量付费（很便宜）

### 如何查看使用量

1. 在Railway控制台
2. 点击你的账户
3. 查看 "Usage" 标签

### 如果超出免费额度

- 升级到付费计划
- 或使用其他免费平台（Vercel、Render）

---

## 🎯 推荐配置

### 最佳方案（推荐）

- **后端**: Railway
- **前端**: Vercel
- **费用**: 完全免费
- **性能**: 优秀

### 简单方案

- **后端**: Railway
- **前端**: Railway
- **费用**: $5/月免费额度
- **性能**: 良好

---

## ❓ 常见问题

### Q1: 部署失败怎么办？

**检查步骤**：
1. 查看Railway日志
2. 检查Dockerfile是否正确
3. 检查环境变量是否配置
4. 检查GitHub仓库是否正确

### Q2: API请求失败？

**检查步骤**：
1. 确认后端URL是否正确
2. 检查前端meta标签配置
3. 查看浏览器控制台错误
4. 检查CORS设置

### Q3: 如何更新代码？

**步骤**：
```bash
git add .
git commit -m "更新内容"
git push
```
Railway会自动重新部署！

### Q4: 如何查看日志？

在Railway控制台 > 服务 > Deployments > 查看日志

---

## 📚 相关链接

- Railway文档：https://docs.railway.app
- Railway定价：https://railway.app/pricing
- Vercel文档：https://vercel.com/docs

---

**完成！你的应用现在可以被全世界访问了！** 🌍✨

